# Frontend - Disclaimer Checker

Basic React UI for testing the Disclaimer Checker API.

## Usage

Simply open `index.html` in your web browser. No build step required.

Make sure the backend is running on `http://localhost:8000` before using the UI.

## Features

- Upload PDF files
- View analysis results in a new tab
- See approval status, risk level, and detailed analysis
